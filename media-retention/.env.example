# -----------------------------------------------------------------------------
# KALTURA API CREDENTIALS -----------------------------------------------------
# -----------------------------------------------------------------------------
PARTNER_ID=
ADMIN_SECRET=
USER_ID=
PRIVILEGES=all:*,disableentitlement

# -----------------------------------------------------------------------------
# MISCELLANEOUS GLOBAL VARIABLES ----------------------------------------------
# -----------------------------------------------------------------------------
TIMEZONE=America/Los_Angeles  # any valid IANA tz like "America/New_York", "Europe/London"

# -----------------------------------------------------------------------------
# PERFORMANCE KNOBS FOR API CALLS ---------------------------------------------
# (used by media-retention-report.py and include-flavor-calculations) ---------
# -----------------------------------------------------------------------------
PROGRESS_EVERY_SEC=2.0
PROGRESS_STYLE=multiline
HTTP_RETRIES=7
HTTP_TIMEOUT=900
HTTP_BACKOFF_BASE=1.6
HTTP_BACKOFF_MIN=1.0
HTTP_BACKOFF_MAX=12.0
THROTTLE_MS=0
PREVENT_COMPUTER_SLEEP=True


# -----------------------------------------------------------------------------
# VARIABLES FOR media-retention-report.py -------------------------------------
# -----------------------------------------------------------------------------
CUSTOM_METADATA_PROFILE_ID=                               # optional

KMC_EXPORT_ALL_FILENAME=all.csv             			  # required
KMC_EXPORT_QUIZ_FILENAME=quizzes.csv        			  # optional
KMC_EXPORT_YOUTUBE_FILENAME=youtube.csv     			  # optional

REPORT_DATE=

OUTPUT_CSV_FILENAME=

INCLUDE_NONREADY=1                       				  # 0 to exclude non-ready rows
OUTPUT_TIMESTAMP=1                       				  # 0 to disable timestamp suffix

# Script-specific performance knobs
REPORT_LOOKUP_WORKERS=16
PROGRESS_VERBOSE_RETRY=0
FLUSH_EVERY=500


# -----------------------------------------------------------------------------
# VARIABLES FOR include-flavor-calculations.py  -------------------------------
# -----------------------------------------------------------------------------
FLAVOR_INPUT_FILENAME=
FLAVOR_OUTPUT_FILENAME=

# Performance knob (inherits others above)
FLAVOR_LOOKUP_WORKERS=6


# -----------------------------------------------------------------------------
# VARIABLES FOR retention-summary.py ------------------------------------------
# -----------------------------------------------------------------------------
SUMMARY_INPUT_FILENAME=
SUMMARY_OUTPUT_FILENAME=
SUMMARY_OUTPUT_TIMESTAMP=1                               # 0 to disable timestamp suffix

# Optional Additional Calculations (comma-delimited)
SUMMARY_OWNER=
SUMMARY_OWNER_ENDS_WITH=
SUMMARY_OWNER_BEGINS_WITH=
SUMMARY_ENTRY_NAME=
SUMMARY_ENTRY_NAME_ENDS_WITH=
LENGTH_EQUALS=0
LENGTH_LESS_THAN=2,5
LENGTH_GREATER_THAN=10800,36000